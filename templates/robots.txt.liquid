{% comment %}
Imperial Knots - robots.txt
Clean + SEO optimized + correct formatting
{% endcomment %}

# Standard Shopify Rules

{% for group in robots.default_groups %}
{{ group.user_agent }}

{% for rule in group.rules %}
{{ rule }}
{% endfor %}

{% if group.sitemap != blank %}
{{ group.sitemap }}
{% endif %}

{% endfor %}

# ----------------------------------

# CUSTOM SEO RULES (IMPORTANT)

# ----------------------------------

User-agent: *
Disallow: /*?*filter*
Disallow: /*?*sort_by*
Disallow: /*?*variant*
Disallow: /*?*page=
Disallow: /search

# Optional: block encoded/duplicate collection URLs

Disallow: /collections/*+*
Disallow: /collections/*%2B*
Disallow: /collections/*%2b*

# Block unnecessary Shopify system paths

Disallow: /apple-app-site-association
Disallow: /.well-known/shopify/monorail
Disallow: /cdn/wpm/

# Allow key pages

Allow: /collections/
Allow: /products/
Allow: /blogs/
Allow: /pages/

# Sitemap

Sitemap: https://www.imperialknots.com/sitemap.xml

# ----------------------------------

# BOT-SPECIFIC RULES

# ----------------------------------

User-agent: MJ12bot
Crawl-delay: 10

User-agent: Pinterest
Crawl-delay: 1
